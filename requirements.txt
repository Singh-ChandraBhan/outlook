# Runtime and HTTP/TLS
requests>=2.32
truststore>=0.9

# Web UI, CLI, Excel import, and analytics
streamlit>=1.42
typer>=0.12
pandas>=2.2
openpyxl>=3.1

# Azure, Microsoft Graph authentication, storage, search, and Azure OpenAI
azure-identity>=1.17
azure-search-documents>=11.6
azure-storage-blob>=12.22
openai>=1.55

# Outlook Desktop integration (Windows only)
pywin32>=306; sys_platform == "win32"

# Tests
pytest>=8.0
